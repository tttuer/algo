import sys
input = sys.stdin.readline

import heapq
q = []
heapq.heappush(q, a)
heapq.heappop(q)

arr = [[0]*m for _ in range(n)]

if all(v for v in arr):
if any(v%2==0 for v in arr):

a,b = b,a
arr[i],arr[j] = arr[j],arr[i]

from collections import Counter
cnt = Counter(arr)

from collections import defaultdict
d = defaultdict(list)
d[0].append(0)

new = arr[:]
new = list(arr)

